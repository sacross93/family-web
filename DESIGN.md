# 포동(Podong) 디자인 시스템 · 일관성 가이드

우리 가족 공간 **포동**의 모든 화면은 이 문서를 따릅니다.
새 화면·기능을 만들 때 **여기 정의된 토큰과 컴포넌트만 사용**하세요.
새로운 색·간격·그림자를 즉흥적으로 만들지 않습니다. 일관성이 곧 완성도입니다.

> 컨셉 한 줄: **「아기자기한 핑크 파스텔」** — 두 사람이 셋이 되기를 기다리는 집.
> 바탕은 아주 옅은 핑크 종이, 내용은 흰 판, 틀은 연한 로즈.
> 화면 전체가 밝으니 위계는 상자도 명도도 아닌 **글자와 여백**이 만든다.

> **왜 이렇게 됐나.** 첫 판은 "냉장고 문 / 코르크보드" 였는데, 실제로 만들어진 것은
> 13개 화면에 걸친 **똑같은 흰 카드 더미**였다 — 같은 모서리, 같은 그림자, 같은 여백.
> 무엇이 중요한지 화면이 말해 주지 못했고, 가장 중요한 "8주 1일" 이 목록 제목보다
> 크지 않았다. 다음 판은 **틀을 진한 자두로** 칠해 화면을 밝고 어두운 둘로 갈랐다.
> 지금 판은 그 틀을 **연한 로즈**로 바꿨다 — 아기자기한 파스텔을 얻는 대신 명도로
> 가르던 방법을 잃었으므로, 위계는 남은 둘이 떠받친다: **판을 종류별로**,
> **과감함은 한 곳에만**.

---

## 1. 디자인 원칙

1. **과감함은 한 곳에만 쓴다.** 화면마다 기억에 남을 것은 하나다 — 아기 페이지는 주차 숫자,
   홈은 오늘 인사. 나머지는 조용히 뒤로 물러난다. 강조가 둘이 되면 강조가 없는 것과 같다.
2. **위계는 상자가 아니라 글자와 여백이 만든다.** 무언가를 돋보이게 하려고 테두리·그림자를
   더하지 않는다. 크기를 키우고 둘레를 비운다.
3. **색은 정보다.** 파스텔 6색은 분류를 뜻한다(장보기 분류, 아기 색, 구성원). 장식이 아니다.
   같은 색이 두 가지를 뜻하면 색은 아무것도 뜻하지 않게 된다.
4. **없는 것은 자리를 덜 차지한다.** 빈 카드가 가득 찬 카드와 같은 높이를 갖지 않는다.
   그래도 초대는 남긴다.
5. **폰이 먼저다.** 390px 에서 손으로 만져 보고 고친다. 손 얹어야 나타나는 것을 만들지 않는다.
6. **하나를 덜어낸다.** 다 만든 뒤 화면에서 장식 하나를 뺀다. 워시테이프도 그렇게 뗐다.

---

## 2. 색 (Color)

값의 단일 출처는 `app/globals.css` 의 `@theme`. 코드에서 HEX 를 쓰지 않는다.

### 바탕 · 판 · 틀

| 토큰 | 값 | 무엇 |
|---|---|---|
| `paper` | `#fbeaf2` | 페이지 바탕 (옅은 핑크 종이) |
| `surface` | `#ffffff` | 내용 판 |
| `sunken` | `#f9e2ec` | 판 안에서 한 단 들어간 자리 |
| `chrome` | `#f6cedd` | **틀** — 사이드바·상단바·탭바·히어로 (로즈) |
| `chrome-soft` | `#f1bed1` | 틀 위에서 한 단 뜬 자리 |
| `chrome-ink` | `#3e2f36` | 틀 위 글자 (8.9:1) |
| `chrome-faint` | `#6e5560` | 틀 위 흐린 글자 (4.7:1) |
| `line` / `line-strong` | `#f2dce6` / `#e8c8d8` | 실선 |

**세 단계는 눈에 보일 만큼 벌려 둔다.** 처음 핑크로 칠했을 때 흰 판 vs 종이가 **1.078:1**
이라 목록이 배경에 녹아 어디까지가 한 판인지 안 보였다. 지금은 1.157(판↔바탕) ·
1.227(바탕↔틀). 대비 기준(4.5/3)은 **글자** 이야기라 넓은 면에는 안 맞지만,
1.08 은 사람 눈으로도 아래다. `lib/colors.contrast.test.ts` 가 이 둘을 지킨다.

**히어로는 평평하지 않다** — `bg-gradient-to-br from-chrome via-chrome to-peach-soft`.
글자가 양 끝 어디에 놓일지 모르므로 대비는 **두 끝에서** 잰다.

틀이 밝아지면서 **예전 규칙이 세 가지 뒤집혔다.** 진한 틀을 전제로 쓴 코드가 남아 있으면
조용히 사라지는 쪽이라, 새로 그릴 때 아래를 본다.

- **틀 위에서 눈에 띄어야 하는 것은 흰 판이나 브랜드 로즈로 채운다.**
  탭바의 포동이 버튼은 `bg-primary text-white`(4.9:1), 켜진 탭은 `bg-surface`.
  `chrome-soft` 로 채우면 사라진다 — 틀 위에서 1.1:1 이다.
- **`ink` 세 벌을 그대로 옮기지 않는다.** 틀이 밝다고 `text-ink-faint` 를 틀 위에 쓰면
  AA 에 못 미친다. 틀 위 글자는 언제나 `chrome-ink`·`chrome-faint`.
- **반투명 흰색을 틀 위에 깔지 않는다.** 진한 틀 시절의 `bg-white/10` 은 로즈 위에서
  1.0:1 — 있으나 마나다. 채움이 필요하면 흰 판(`bg-surface`),
  테두리·구분선은 `border-ink/10~20`.
  `line`·`line-strong` 도 안 된다 — 둘 다 옅은 핑크라 이 로즈 위에서 사라진다.
- **분류색 여섯(`-soft`)으로 틀 위를 칠하지 않는다.** 전부 1.09~1.20:1 이라
  켜진 탭이 켜져 있는지 알 수 없다. 분류색은 **흰 판 위에서** 제 몫을 한다.

### 글자

`ink`(#3e2f36) · `ink-soft`(#5e4650) · `ink-faint`(#7a606c).
흰 판·바탕·들어간 자리 어디에서도 4.5:1 을 넘는다. 더 옅게 만들고 싶으면
`lib/colors.contrast.test.ts` 부터 볼 것.

### 브랜드 · 강조

- `primary` `#be4571` — 주요 액션(로즈). 흰 글자가 4.9:1.
  밝은 판에 **글자로** 쓸 때는 `primary-ink`(`#9c3459`) — `primary` 자체는 흰 판에서는
  AA 를 넘지만 들어간 자리(4.2)·틀(3.8) 위에서는 못 넘어, 어디에 놓일지 모르는 채로는 쓸 수 없다.
- `accent` `#b35f18` — **"지금 벌어지는 일" 에만.** 오늘 날짜, 남은 날, 진행.
  **글자로 쓰지 않는다**(흰 판에서 4.6:1 이지만 틀 위에서는 3.2). 글자가 필요하면 `accent-ink`(`#8a4410`).
  그래픽으로 쓸 때 틀 위 3.2:1 — UI 기준 3:1 에 여유가 크지 않으니 값을 건드리면 검사부터 볼 것.

### 파스텔 6색 (lavender·peach·mint·sky·butter·rose)

각 색은 `-soft`(배경) · `-ink`(글자) · 기본(점·막대)의 세 벌. 분류에만 쓴다.
`lib/colors.ts` 의 `palette(key)` 로만 접근한다 — `"bg-" + key` 같은 동적 조합은
Tailwind v4 가 스캔하지 못한다.

---

## 3. 글꼴 (Typography)

| 역할 | 글꼴 | 어디에 |
|---|---|---|
| 제목 | **주아** (둥글고 도톰한 한글) `font-display` | 히어로 숫자, 페이지·판 제목, 브랜드 |
| 본문·UI | **Pretendard** `font-sans` | 그 밖 전부 |
| 숫자 | Pretendard + 고정폭 자릿수 `.font-num` | 날짜·개수·D-day |

- **제목 글꼴은 18px 아래로 내려가지 않는다.** 주아는 굵기가 하나뿐이고 도톰해서 작으면 뭉친다.
  목록의 날짜·개수는 `.font-num`(고딕)이다 — `font-display` 를 상속시키지 않는다.
- 큰 제목은 `leading-tight` + `break-keep`. 한글은 단어 중간에서 끊기면 읽기가 느려진다.
- 제목의 **한 단어만 색칠하지 않는다.** 강조가 흩어지면 아무것도 강조되지 않는다.
- 대문자 라벨(`ALL CAPS`), 가운뎃점으로 이은 메타 줄(`A · B · C`), 버튼 끝 화살표(`→`),
  작은 라벨용 고정폭 글꼴 — 쓰지 않는다. 어느 사이트에나 붙는 기본값이다.

---

## 4. 여백 · 모서리 · 그림자

- **여백**: Tailwind 4배수. 판 안쪽 `p-5`, 덩어리 사이 `gap-4`~`gap-6`.
- **모서리는 크기가 아니라 무엇인가로 고른다.**

  | 토큰 | 값 | 무엇 |
  |---|---|---|
  | `rounded-full` | — | 칩·버튼·아바타 |
  | `rounded-sm` | 10px | 판 안의 작은 조각 |
  | `rounded-md` | 16px | 사진, 채운 타일 |
  | `rounded-lg` | 20px | 내용 판 (`Card` 기본) |
  | `rounded-xl` | 28px | 히어로처럼 큰 덩어리 |

- **그림자는 얕게, 로즈 기운으로.** 밝은 핑크 바탕에서는 흰 판이 바탕과 **1.08:1** 이라
  테두리만으로는 판이 보이지 않는다 — 진한 판이던 시절 "그림자는 판에 쓰지 않는다" 고
  정했던 규칙이 여기서는 반대로 작동한다. 대신 **회색 그림자는 쓰지 않는다**:
  파스텔 위에서 때처럼 보인다. 판은 `--shadow-card` 하나만, 정말로 떠 있는 것
  (모달·메뉴·알림)은 `--shadow-md` / `--shadow-lg`.

---

## 5. 컴포넌트 (Component)

모든 화면은 아래 공용 컴포넌트로 조립합니다. **import는 배럴에서**:

```tsx
import {
  Button, Card, CardTitle, Tag, ColorDot, PageHeader, EmptyState,
  Field, Label, Input, Textarea, Select, Checkbox, ColorPicker,
  Avatar, IconButton, Modal, Spinner, LoadingBlock, Segmented,
  ItemActions, CollapsibleCard, useConfirm, useToast, useFocusTrap,
} from "@/components/ui";
```

**`Card` 는 세 종류다.** 무엇을 담는가로 고른다.

| variant | 모양 | 담는 것 |
|---|---|---|
| `plain`(기본) | 흰 판 + 실선 한 겹 | 목록·폼처럼 여러 가지가 들어가는 판 |
| `tile` | 색으로 채움, 테두리 없음 | 하나만 담는 덩어리(주차 숫자, 오늘 요약) |
| `photo` | 여백·테두리 없음 | 사진 |

| 컴포넌트 | 용도 | 핵심 props |
|---|---|---|
| `Button` | 액션 | `variant`: primary·soft·ghost·outline·danger / `size` / `href` |
| `IconButton` | 아이콘 전용 버튼 | `variant`: ghost·soft·danger·surface / `size` |
| `Card` | 내용 판 | `variant` / `interactive` / `flush` |
| `CardTitle` | 판 제목 (제목 글꼴 18px) | — |
| `Tag` | 색상 태그·뱃지 | `color`(팔레트 키) / `dot` |
| `PageHeader` | 페이지 상단 | `emoji` `title` `description` / children=우측 액션 |
| `EmptyState` | 빈 상태 | `emoji` `title` `description` `action` |
| `CollapsibleCard` | 접었다 펴는 판 | `emoji` `title` `summary` `defaultOpen` |
| `ItemActions` | `…` 메뉴 | `actions` / `inline` |
| `Modal` | 다이얼로그 | `open` `onClose` `title` `emoji` `footer` `size` |
| `Segmented` | 뷰/필터 전환 | `value` `options` `onChange` |
| `useConfirm` / `useToast` | 묻기 / 알리기 | §8 참고 |

아이콘은 **`lucide-react`**(기능: 닫기·추가·수정·삭제) + **이모지**(콘텐츠 성격: 🌴 여행, 🎂 생일).

---

## 6. 레이아웃 패턴

- **앱 셸** (`components/app-shell.tsx`) — 틀은 전부 `bg-chrome` 한 색이다.
  - 데스크톱: 좌측 사이드바 264px (연한 로즈 틀).
  - 폰: **연한 로즈 상단바 + 같은 색 하단 탭바**. 상단바는 지금 페이지의 이모지·제목을 말하고
    (브랜드는 홈에서만), 메뉴는 아래 탭바에 있다 — 엄지가 닿는 자리다.
    탭은 `getNav()` 앞 `TAB_COUNT`개 + `더보기`. **탭 목록을 따로 만들지 않는다**(`lib/nav.ts` 한 곳).
  - 틀 안에서는 `on-chrome` 클래스를 붙인다 — 포커스 링이 틀 위 글자색으로 바뀐다.
    기본 로즈 링은 연한 로즈 틀 위에서 3.8:1 로 흐릿하다(같은 계열 위의 같은 계열).
- **아래쪽 가장자리**: `--bottom-bar`(globals.css) 한 곳에서 온다. 탭바 높이 + iOS 안전영역.
  페이지에서 `pb-*` 를 손으로 맞추지 않는다.
- **떠 있는 버튼을 만들지 않는다.** 화면 한가운데에 떠 있는 버튼은 그 자리의 콘텐츠를
  누를 수 없게 만든다 — 아기 기록의 `…` 가 `물어보기` FAB 밑에 깔려, 일기를 고치려고
  누르면 AI 채팅이 열렸다. 포동이는 탭바 안으로, 꾸미기는 상단바 안으로 들어가 있다.
- **더하는 버튼은 더해지는 목록 옆에 둔다.** 페이지 맨 위 빈 줄에 버튼만 띄우지 않는다 —
  폰에서는 제목이 상단바로 올라가면서 그 줄에 버튼 하나만 남아, 무엇에 더하는지 알 수 없다.
- **사진은 글자 위에 겹치지 않는다.** `absolute` 로 얹지 말고 나란히 놓는다.
  홈 히어로의 사진이 `-right-4 -top-4` 였을 때 폰에서 인사말 둘째 줄을 통째로 덮었다.
- **콘텐츠 폭**: `max-w-6xl` 중앙 정렬.
- **그리드**: 카드 목록은 `grid gap-4 sm:grid-cols-2 lg:grid-cols-3` 류. 모바일 1열 필수.

---

## 7. 데이터 · 상호작용 패턴 (개발 규칙)

일관된 구조로 병렬 작업이 안전하도록 아래 패턴을 **반드시** 따릅니다.

```
app/<feature>/page.tsx            # 서버 컴포넌트: prisma로 초기 데이터 read → 클라이언트에 전달
app/<feature>/<feature>-client.tsx# "use client": 상태 보유(초기값=서버 props), UI + 상호작용
app/api/<feature>/route.ts        # GET(목록) · POST(생성)
app/api/<feature>/[id]/route.ts   # PATCH(수정) · DELETE(삭제)
```

- **읽기**: 서버 컴포넌트에서 `import { prisma } from "@/lib/prisma"` 직접 사용.
- **쓰기**: 클라이언트가 `fetch("/api/...")` 호출. 낙관적으로 로컬 상태를 먼저 갱신하고,
  실패 시 되돌립니다(간단히는 성공 후 `router.refresh()`도 가능).
- **응답**: API는 `NextResponse.json(data)` 반환. 날짜는 ISO 문자열로 직렬화됨 →
  클라이언트에서 `new Date()`로 파싱.
- **색상 필드**는 팔레트 키 문자열(`"mint"` 등)로 저장/전달.

날짜 표기는 `lib/date.ts` 헬퍼 사용: `kDate`, `kDateShort`, `kTime`, `dday`, `ageFrom` 등.

---

## 8. 보이스 & 톤 (Voice & Tone)

가족에게 말하듯 **따뜻하고 담백하게**. 사용자가 조작하는 것을 그 이름으로 부릅니다.

- 버튼은 결과를 말한다: "저장" → 토스트도 "저장했어요". "제출"❌ "저장"⭕
- 빈 화면은 초대다: "아직 사진이 없어요. 첫 추억을 올려볼까요? 📸"
- 오류는 사과하지 않고 방법을 알려준다: "사진을 못 올렸어요. 잠시 후 다시 시도해 주세요."
- **실패를 조용히 되돌리지 않는다.** 낙관적 업데이트를 되돌릴 때는 반드시 `useToast()` 로
  한 줄 알려 준다(`const { say } = useToast()`). 아무 말 없이 항목이 사라지거나 되살아나면
  가족은 자기가 잘못 눌렀다고 생각한다.
  네트워크가 끊기면 `fetch` 는 **거부**된다 — `if (!res.ok)` 만으로는 안 잡히니 `catch` 도 둘 것.
- **지우기 전에 묻는 기준은 "다시 만들 수 있는가"다.** 장보기 항목·할일은 3초면 다시 담으니
  묻지 않는 쪽이 빠르다. 가족이 쓴 글·아기 기록·포동이와 나눈 대화·사진은 다시 만들 수 없으니
  `useConfirm()` 으로 한 번 묻는다(`components/ui/confirm-dialog.tsx`). `window.confirm` 은
  쓰지 않는다 — 사이트의 말투도 모양도 따르지 않고, 무엇이 사라지는지 적을 자리도 없다.
- 문장은 짧게, 존댓말, 이모지는 양념처럼 한두 개.
- 라벨 예시: `+ 앨범 만들기`, `+ 할일 추가`, `오늘 할일`, `다가오는 일정`, `D-3`.

---

## 9. 접근성 (Accessibility) — 품질 하한선

- 모든 인터랙티브 요소에 보이는 **포커스 링**(globals.css 기본 제공).
- **떠 있는 판(모달·시트·드로어)은 탭을 가둔다** — `useFocusTrap(open)` 을 쓰고
  판에 `tabIndex={-1} role="dialog" aria-modal="true"` 를 준다. 가두지 않으면 폼이
  떠 있는 채로 Tab 이 **보이지도 않는 뒤쪽 화면으로 새어 나가** 엉뚱한 데서 엔터가 눌린다.
  닫을 때는 열기 전 자리로 포커스를 돌려준다. `npm run ui:audit` 이 확인한다.
- 아이콘 전용 버튼엔 `aria-label`.
- **목록 항목의 `Checkbox` 에는 그 항목 이름을 `label` 로 준다** — 이름이 없으면 스크린리더에
  "체크박스" 라고만 읽혀 무엇을 켜는지 알 수 없다. 여러 줄이 같은 "완료" 를 갖는 것도 같은 문제다.
- 색만으로 정보 전달 금지 — 색 + 텍스트/아이콘 병행(태그엔 라벨 필수).
- **글자 대비는 재서 정한다**(`lib/colors.contrast.test.ts`가 토큰 값으로 확인).
  흰 배경·종이 배경에서 `ink`·`ink-soft`·`ink-faint` 셋 다 **4.5:1 이상**.
  예전 `ink-faint`(#A6A6B2)는 2.41:1 이라 날짜·개수·캡션이 사실상 안 읽혔다.
  더 옅게 만들고 싶으면 대비부터 재 볼 것 — 세 단계 간격도 같이 본다(한 덩어리가 되면 위계가 사라진다).
  **틀 위도 같이 잰다**: `chrome-ink`·`chrome-faint` 가 `chrome` 위에서 4.5:1 을 넘는지.
  틀이 연한 로즈로 밝아졌어도 밝은 판의 글자색을 그대로 옮기면 안 읽힌다 —
  `ink-faint` 는 틀 위에서 **4.34:1** 로 AA 에 못 미친다. `chrome-faint`(5.2)가 따로 있는 이유다.
  `accent` 는 **글자로 쓰지 않는다**(흰 판 3.9:1) — 밝은 판에 글자가 필요하면 `accent-ink`.
  글자색은 **놓일 수 있는 배경 전부**에서 잰다: 틀이 밝아지며 배경이 흰 판·종이·틀 셋으로
  늘었고, 흰 판만 보면 통과하지만 틀 위에서 떨어지는 값이 생긴다(검사가 그 경우를 잡는다).
  검사는 값을 바꿔 **일부러 깨뜨려 보고** 남긴다. 안 깨지는 검사는 아무것도 지키지 못한다.
- **모바일 탭 타깃 ≥ 40px** — `npm run ui:audit` 가
  보이는 크기가 아니라 **실제로 눌리는 넓이**를 잰다.
  - 버튼·칩은 폰에서 `h-10`(데스크톱은 `lg:h-8`처럼 낮춰도 된다. 마우스는 정확하다).
  - 체크 동그라미처럼 **작아야 예쁜 것**은 `.tap-target`(globals.css)을 붙인다 —
    보이는 크기는 그대로 두고 보이지 않는 넓이만 44px 로 넓힌다. 폰에서만 적용되므로
    데스크톱에서 나란한 아이콘끼리 영역이 겹치지 않는다.
  - 글자 링크는 `-my-3 py-3` 처럼 자리를 뺏지 않고 높이만 키운다.
- `prefers-reduced-motion` 존중(globals.css에서 애니메이션 억제).
- **글자 크기는 `rem` 으로** — `text-[15px]` 처럼 px 로 박으면 브라우저·폰의 "글자 크기"
  설정을 따라가지 않는다. `text-[0.9375rem]` 처럼 쓰거나 Tailwind 기본(`text-sm` 등)을.
  장식용 이모지(120px 이상)만 예외 — 같이 커지면 화면을 삼킨다.
- **글자만 커져도 읽혀야 한다.** 화면 확대(zoom)와 달리 글자만 커지면 한 줄에 밀어 넣은
  레이아웃이 깨진다 — 장보기 "우유" 가 "두" 로 잘렸다. 좁아지면 아래로 내려가게 둘 것
  (`flex-wrap` + `basis-*`). `npm run ui:audit` 이 1.5배로 확인한다.

---

## 10. Do / Don't

✅ **Do**
- 공용 컴포넌트와 팔레트 토큰만 사용
- 색은 분류에, 과감함은 화면당 한 곳에
- 판을 담는 것에 맞춰 고르기(`plain`·`tile`·`photo`)
- 빈 상태·로딩·에러를 항상 다정하게 처리

🚫 **Don't**
- 임의의 HEX/그림자/폰트 추가
- `bg-${key}-soft` 처럼 동적 클래스 조합 (Tailwind 가 못 잡음)
- 판에 그림자 달기, 카드마다 호버 부양
- 대문자 라벨 · 가운뎃점 메타 줄 · 버튼 끝 화살표 · 제목 한 단어만 색칠
- 떠 있는 버튼, 손 얹어야만 나타나는 액션

---

## 부록. 토큰 값을 바꿀 때 — 이름이 같아도 크기는 달라진다

`@theme` 에서 `--radius-*` 를 다시 잡으면 **그 이름을 쓰던 모든 자리가 같이 움직인다.**
Tailwind 기본값을 쓰던 자리도 포함이다. 개편 1회차에서 실제로 이렇게 됐다:

| 이름 | 전(Tailwind 기본) | 후(우리 값) | 조용히 벌어진 일 |
|---|---|---|---|
| `rounded-xl` | 12px | 28px | 36px 짜리 이모지 타일 15개가 **거의 동그라미**가 됐다 — 이 사이트에서 동그라미는 '사람' 을 뜻하는데 |
| `rounded-lg` | 8px | 20px | 말풍선 꼬리(`rounded-br-lg`)가 **사라졌다** — 28px 모서리에 20px 꼬리는 꼬리가 아니다 |
| `rounded-md` | 6px | 16px | 달력의 18px 짜리 일정 칩이 알약이 됐다 |

셋 다 깨진 것이 아니라 **조금씩 달라진 것**이라 눈에 잘 안 띈다. 그래서 규칙:

- 토큰 값을 바꾸면 **그 이름을 쓰는 자리를 전부 훑는다**
  (`git grep rounded-xl <바꾸기 전 커밋>`).
- 작은 조각(타일·칩·꼬리)은 큰 판과 **같은 이름을 쓰지 않는다**.
  이 저장소의 지금 짝: 칩·꼬리 `sm`(10px) · 타일·사진 `md`(16px) · 판 `lg`(20px) ·
  히어로·모달 `xl`(28px) · 버튼·아바타 `full`.
- `rounded-2xl`·`rounded-3xl` 처럼 **토큰 밖 이름은 쓰지 않는다**. 값이 같아 보여도
  나중에 토큰을 바꿀 때 거기만 따라오지 않는다.

---

## 부록 2. 이 문서가 거짓말하지 않게

`AGENTS.md` 는 "새 화면/기능 전에 DESIGN.md 필독" 이라고 못박는다. 그런데 팔레트를 두 번
바꾸는 동안 위의 색 표가 **한 판 전 값에 머물러 있었다** — 진한 자두 → 핑크 때 한 번,
세 단계를 벌릴 때 또 한 번. **필독 문서가 거짓말을 하면 안 읽는 것만 못하다.**

그래서 `lib/design-doc.test.ts` 가 이 문서에 적힌 `토큰` ↔ `#hex` 짝을 전부 걷어
`app/globals.css` 와 대조한다. 어느 쪽을 고치든 나머지를 안 고치면 **줄 번호까지 찍어**
걸린다. 색 토큰을 바꾸면 `npm test` 가 여기로 데려다줄 것이다.
